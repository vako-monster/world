#include<stdio.h>

int main(){
	int n,count=1;
	double h,sum=0;
	scanf("%lf %d",&h,&n);
	sum=h;
	while(count<n&&n!=0){
		h=h*0.5;
		sum+=2*h;
		count++;}
	  if(n==0){
        sum=0;
        h=0;
     }
     
	h=h*0.5;
	printf("%.1lf %.1lf",sum,h);
   
    
   

	return 0;
} 
