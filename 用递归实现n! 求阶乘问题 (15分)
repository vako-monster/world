#include<stdio.h>
double fact(int n);
int main(){
	int n;
	scanf("%d",&n);
	printf("%.0f",fact(n));
	return 0;
}
double fact(int n){
	if(n==1)
	return 1;
	else if(n==0)
	return 0; 
   	else if(n>0)
	return n*fact(n-1);
}
