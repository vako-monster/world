#include <stdio.h>

int main()
{
    int n,x,i,a[10],j=1;
    scanf("%d %d",&n,&x);
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
        if(x==a[i]){
            printf("%d\n",i);
            j=0;
        }
    }
    if(j){
        printf("Not Found");
    }
    return 0;
}
