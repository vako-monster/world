#include<stdio.h>
#include<string.h> 
int main(){
	int i,j=-1;
	char ch[90];
    char c,b;
c=getchar();
b=getchar();
gets(ch);
int l=strlen(ch);
for(i=0;i<l;i++){

if(ch[i]==c){
	j=i;
}
}
if(j==-1)
printf("Not Found");
else {
	printf("index = %d",j);
}
return 0;}
