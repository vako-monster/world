#include<stdio.h>
#include<math.h>
int main(){
	int n,i,p,sum=0,count=0;

	scanf("%d",&n);
	for(i=1;i<=n;i++){
		sum=pow(2,i)-1;
	for(p=2;p<=sum/2;p++)
		if(sum%p==0)
		break;
	    if(p>sum/2&&sum!=1){
		
	    printf("%d\n",sum);
	    count++;}}
	if(count==0)
	printf("None");
	
	
	return 0;
} 
