#include <stdio.h>
#include <math.h>

int prime( int p );
int PrimeSum( int m, int n );

int main()
{
    int m, n, p;

    scanf("%d %d", &m, &n);
    printf("Sum of ( ");
    for( p=m; p<=n; p++ ) {
        if( prime(p) != 0 )
            printf("%d ", p);
    }
    printf(") = %d\n", PrimeSum(m, n));

    return 0;
}
int prime( int p ){
	int a=1;
	if(p<=1)
	a=0;
	for(int i=2;i<p;i++){
		if(p%i==0)
		a=0;
	}
	return a;
}
int PrimeSum( int m, int n ){
	int sum=0;
	for(;m<=n;m++){
		if(prime(m))
		sum+=m;
	}
	return sum;
}
