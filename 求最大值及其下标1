#include<stdio.h>
int main(){
	int n,x,i,sum=0;
	scanf("%d",&n);
	int a[10];

	for(i=0;i<n;i++)
		scanf("%d",&a[i]);
		for(i=1;i<n;i++)
		
		if(a[sum]<a[i])
		sum=i;
		printf("%d %d",a[sum],sum);
		


		return 0;}
		
	
    
