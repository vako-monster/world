#include <stdio.h>
#include <string.h>

// 字符串长度
int StrLen(const char *str);

int main()
{
    char a[1024];
    int n;
    gets(a);
    n = StrLen(a);
    printf("%d\n", n);
    return 0;
}
int StrLen(const char *str){
	int i,count=0;
	for(i=0;str[i]!='\0';i++){
		count++;
	}
	return count;
}
