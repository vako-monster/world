#include<stdio.h>
int main(){
	int n,x,i,sum=0;
	scanf("%d",&n);
	int a[10];

	for(i=0;i<n;i++)
		scanf("%d",&a[i]);
		for(i=1;i<n;i++)
		
		if(a[sum]>a[i])
		sum=i;
		
		int temp;
		temp=a[sum];
		a[sum]=a[0];
		a[0]=temp;
		for(i=0;i<n;i++)
		printf("%d ",a[i]); 
		


		return 0;}
		
	
    
